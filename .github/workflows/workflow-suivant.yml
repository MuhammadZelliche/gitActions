name: Workflow Suivant

on:
  workflow_run:
    workflows: ["Merge PR"]
    types:
      - completed

jobs:
  suivant-job:
    runs-on: ubuntu-latest
    steps:
      - name: Message
        run: echo "Workflow suivant exécuté ✅"
